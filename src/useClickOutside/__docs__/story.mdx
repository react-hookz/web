import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Example } from './example.stories'
import { ImportPath } from '../../__docs__/ImportPath'

<Meta title="DOM/useClickOutside" component={Example} />

# useClickOutside

Triggers a callback when the user clicks outside a targeted element.

> This hook listens for events on the `document` object. Therefore, if any listener between the event
> target and the `document` object stops event propagation, the hook won't work.

> By default, the `mousedown` and `touchstart` events are used, but any bubbling events can be used instead.

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
const DEFAULT_EVENTS = ['mousedown', 'touchstart'];

export function useClickOutside<T extends HTMLElement>(
  ref: RefObject<T> | MutableRefObject<T>,
  callback: EventListener,
  events: string[] = DEFAULT_EVENTS
): void;
```

#### Importing

<ImportPath />

#### Arguments

- **ref** _`RefObject<T> | MutableRefObject<T>`_ - React ref object containing the target HTML element.
- **callback** _`EventListener`_ - Callback invoked when the user clicks outside the target HTML element.
- **events**: _`string[]`_ _(default: `['mousedown', 'touchstart']`)_ - List of events that will be
  used as triggers for the outside click.
