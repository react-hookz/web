import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { Example } from './example.stories';
import { ImportPath } from '../../storybookUtil/ImportPath';

<Meta title="Lifecycle/useLogger" component={Example} />

# useEffectOnce

Console log when component is mounted and when the component is updated.

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
function useLogger(effect: CallableFunction): void;
```

#### Importing

<ImportPath />

#### Arguments

- _**effect**_ _`CallableFunction`_ - Effector to create a log when the component mounts and updates.
