import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { Example } from './example.stories';
import { ImportPath } from '../../storybookUtil/ImportPath';

<Meta title="State/useQueue" component={Example} />

# useQueue

A react state hook implements simple first-in first-out (FIFO) queue.

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
export function useQueue<T[]>(initialValue: T[] =[]): {
  add: (item: T) => void;
  remove: () => T;
  first: T;
  size: number;
}
```

#### Importing

<ImportPath />

#### Arguments

- _**initialValue**_ _`T[]`_ - The initial value of the queue state. Defaults to an empty array.

#### Return
