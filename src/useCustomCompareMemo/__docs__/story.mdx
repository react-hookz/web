import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Example } from './example.stories';
import { ImportPath } from '../../__docs__/ImportPath';

<Meta title="Miscellaneous/useCustomCompareMemo" component={Example} />

# useCustomCompareMemo

Like useMemo but uses provided comparator function to validate dependency changes.

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
export type IDependenciesComparator<Deps extends DependencyList = DependencyList> = (
  a: Deps,
  b: Deps
) => boolean;

function useCustomCompareMemo<Factory extends () => unknown, Deps extends DependencyList>(
  factory: Factory,
  deps: Deps,
  comparator: IDependenciesComparator<Deps>
): ReturnType<Factory>;
```

#### Importing

<ImportPath />

#### Arguments

- **factory** _`() => unknown`_ - useMemo factory function.
- **deps** _`DependencyList`_ - useMemo dependency list.
- **comparator** _`IDependenciesComparator<DependencyList>`_ - comparator function to validate dependency changes.
