import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Example } from './example.stories';
import { ImportPath } from '../../__docs__/ImportPath';

<Meta title="Miscellaneous/useCustomCompareMemo" component={Example} />

# useCustomCompareMemo

Like `useMemo` but with a callback to defines that factory function can be invoked.

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
type DepsAreEqual<Deps extends DependencyList> = (savedDeps: Deps, deps: Deps) => boolean;

function useCustomCompareMemo<Factory extends () => unknown, Deps extends DependencyList>(
  factory: Factory,
  deps: Deps,
  depsAreEqual: DepsAreEqual<Deps>
): ReturnType<Factory>;
```

#### Importing

<ImportPath />

#### Arguments

- **factory** _`() => unknown`_ - useMemo factory function.
- **deps** _`DependencyList`_ - useMemo dependency list.
- **depsAreEqual** _`DepsAreEqual<DependencyList>`_ - defines that factory function can be invoked.
