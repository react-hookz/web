import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { Example } from './example.stories';
import { ImportPath } from '../../storybookUtil/ImportPath';

<Meta title="Dom/useFavicon" component={Example} />

# useFavicon

Sets the favicon of the page.

#### Example

<Canvas>
  <Story story={Example} inline />
</Canvas>

## Reference

```ts
function useFavicon(href: string): void;
```

#### Importing

<ImportPath />

#### Arguments

- **href** _`string`_ - favicon source string to set.
